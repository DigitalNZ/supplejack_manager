//= link_tree ../images
//= link_tree ../builds

// the following line should avoid us to copy the sort_*.png images
// and overwrite the background-image in _datables.scss. But rails
// doesn't find the images for some reasons
//= link_tree ../../../node_modules/datatables.net-zf/css
