<h1 class="left">Parser configurations</h1>
<%= link_to "New Parser configutation", new_parser_path, class: "button new-right" %>

<table id="parsers" class="twelve">
  <thead>
    <tr>
      <th>Name</th>
      <th>Strategy</th>
      <th>Last Updated</th>
      <th>Last Edited By</th>
    </tr>
  </thead>

  <tbody>

    <% @parsers.each do |parser| %>
    <tr>
      <td><%= link_to parser.name, edit_parser_path(parser) %></td>
      <td><%= parser.strategy %></td>
      <td><%= parser.versions.last.try(:created_at).try(:localtime) %></td>
      <td><%= parser.last_edited_by %></td>
    </tr>
    <% end %>
  </tbody>
</table>