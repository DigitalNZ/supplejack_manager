<%= simple_form_for @link_check_rule, url: url do |f| %>
 <%= f.input :source_id, as: :grouped_select, collection: Partner.all.sort(name: 1), group_method: :sources, label_method: :name, group_label_method: :name %>
  <%= f.input :xpath %>
  <%= f.input :status_codes %>
  <%= f.input :throttle %>
  <%= f.input :active, :as => :boolean, value: true %>

  <%= f.button :submit %>
<% end %>