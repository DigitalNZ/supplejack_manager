<%-
require 'csv'

csv_output = CSV.generate(:col_sep => ",") do |csv|
  columns = %w[username name authentication_token email role daily_requests monthly_requests]
  csv << columns

  @users.each do |user|
    csv << columns.map { |column| user[column] }
  end
end
%>

<%= csv_output.html_safe %>
