<h1>New shared module</h1>

<%= simple_form_for @shared_module do |f| %>

  <%= f.input :name %>
  
  <%= f.input :data, as: :text, label: false, required: false, input_html: {class: "code-editor"} %>
  <%= f.input :message, label: t("commit_message") %>

  <%= f.button :submit %>

<% end %>
